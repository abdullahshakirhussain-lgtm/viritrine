node_modules/
server/data/*.db
server/data/*.db-journal
.env
.DS_Store

server/data/*.db-shm
server/data/*.db-wal

# Competitor import artifacts — local-only drafts (pair with the gitignored DB).
# These are the competitors' copyrighted images; never commit/publish them.
server/data/uploads/products/imp-*
scratch_*.log

# Compiled from public/src/**/*.jsx by scripts/build.js (built on deploy).
# motion.js is a hand-written ES module, not build output — keep it tracked.
public/src/**/*.js
!public/src/motion.js
